// 1??2?? Uso de While: Crie um loop que exiba números pares de 2 a 20.
#include "protheus.ch"

User Function Q12() // u_Q12()

    Local nCount

    For nCount := 0 to 20 Step 2
        Alert(nCount, "Números Pares")
    Next
return
