// 5?? Manipulação de Arrays: Crie um array com { "TOTVS", "ERP", "ADVPL" } e exiba cada valor.
#include "protheus.ch"

User Function Q5() // u_Q5()

    Local aArray := {'TOTVS', 'ERP', 'ADVPL'}

    Alert(aArray[1])
    Alert(aArray[2])
    Alert(aArray[3])

return
