// 1️⃣7️⃣ Ordenação de Array: Crie um array com {5, 2, 8, 1} e ordene em ordem crescente.
#include "protheus.ch"

User Function Q17() // u_Q17()

    Local aArray := {5, 2, 8, 1}
 
    ASORT(aArray)

    Alert("O array ordenado é: " + cValToChar(aArray[1]) + " | " + cValToChar(aArray[2]) + " | "  + cValToChar(aArray[3]) + " | " + cValToChar(aArray[4]))

return
